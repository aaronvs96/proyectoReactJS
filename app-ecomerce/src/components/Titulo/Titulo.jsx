import './Titulo.css'

const Titulo = ({ titulo, subTitulo }) => {
    // pbj()
    return (
        //se tiene que agrupar en un solo elemento html, en este caso un div
        <header>
            <div id="logotipo">
                SOMOS MOTOS
            </div>
        </header>
    )
}

export default Titulo